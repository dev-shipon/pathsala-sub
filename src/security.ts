/**
 * Talukdar Pathshala — Content Protection Module (DISABLED)
 */

export function encodeUrl(url: string): string {
    return url; // No encoding
}

export function decodeUrl(encoded: string): string {
    return encoded; // No decoding
}

export function initSecurity() {
    console.log('Security disabled per user request.');
}
